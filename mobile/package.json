{
  "name": "quantsphere-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Native iOS/Android app wrapper for QuantSphere, via Capacitor in remote-URL mode. Wraps the hosted QuantSphere web app in a native shell rather than bundling/rewriting the frontend.",
  "scripts": {
    "doctor": "cap doctor",
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
